# =============================================================================
# bitcoin-gui — Vite Environment Variables Template
# =============================================================================
# This file is safe to commit. It contains no real credentials.
# Copy it to bitcoin-gui/.env and fill in your values:
#   cp .env.example .env
#
# Only VITE_-prefixed variables are exposed to the browser bundle.
# =============================================================================

VITE_RPC_USER=your_rpc_username
VITE_RPC_PASS=your_rpc_password
VITE_RPC_ENDPOINT=http://localhost:8001


